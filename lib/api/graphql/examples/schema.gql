type Book {
  id: ID!
  name: String!
  likes: Int
}

type Author {
  id: ID!
  name: String!
  books: [Book!]
}

type Query {
  book(id: ID!): Book
  author(id: ID!): Author
}

schema {
  query: Query
}
