# ADR-0001: Incremental migration from JavaScript to TypeScript

**Status:** Proposed
**Date:** 2026-07-12
**Deciders:** Kuzzle core team (Ricky — nriquelmebareiro@kuzzle.io), to be validated collectively
**Scope:** Production code (`lib/`, `bin/`, `index.ts`) and unit tests
**Related documents:** [type-debt register](type-debt-register.md) · step files under [`steps/`](steps/)

> Living hub (structure per the `kuzzle-adr` skill). The **Decision** and **Target architecture** are stable; **Cold start** and the **Step table** track live progress; the execution detail of each milestone lives in [`steps/`](steps/). To resume the effort in a fresh context, read **Cold start**.

---

## Decision

### Context

Kuzzle is a mature codebase (v2.56.0, Node ≥20 <25) whose TypeScript migration is **already underway but unfinished and uneven**. It is not _one_ migration but **three intertwined efforts**, which is why it stalls (state at decision time, 2026-07-12):

| Axis                    | Starting state (2026-07-12)                                      | Target         |
| ----------------------- | ---------------------------------------------------------------- | -------------- |
| **1. Language**         | ~50% of `lib/` still JS (94 `.js` files); `bin/` mostly JS       | 100% `.ts`     |
| **2. Unit-test runner** | 168 Mocha specs in JS under `test/`; vitest scaffolded but empty | vitest + TS    |
| **3. Type strictness**  | `strict` off; only `noUncheckedIndexedAccess` on                 | `strict: true` |

Forces at play:

- **Clean interop** — `allowJs: true` + **0 hard `require('./x.js')`** in existing TS, so file-by-file conversion does not break import chains.
- **Safety net** — cucumber functional tests are already 100% TS and cover the critical paths, protecting refactors of the big files.
- **The remaining JS files are the hardest** — the largest, most critical files are still JS (`httpRoutes` 1554 LOC, `httpwsProtocol` 1254, `pluginsManager` 1244, `node` 1203, `validation` 1180, `documentController` 1165, `funnel` 1143) → the core must come **last**, under the functional-test net.
- **Double test debt** — the language axis is blocked by the runner question: converting a Mocha test to `.ts` without moving it to vitest only shifts the debt.
- **Why now** — a durable hybrid costs more than either extreme (double tooling, false-safety partial typing, confusing onboarding), and the un-migrated files are exactly the riskiest.

Full baseline figures: [step 01, Appendix A](steps/01-sprint-0-tooling.md). Options weighed and rejected: [step 00](steps/00-rejected-alternatives.md).

### Decision

Adopt an **incremental migration, driven by a CI ratchet, sequenced by architectural layer**, handling the three axes in a **decoupled but coordinated** way:

1. **Language (JS → TS)** — top priority. Every `.js` in `lib/` and `bin/` becomes `.ts`. A CI ratchet forbids any new `.js` (the count may only decrease). Organised in per-layer sprints, leaves → core, plus the boy-scout rule (convert what you touch).
2. **Strictness (progressive)** — the target is `strict: true`, but it does **not** block the language axis. Strict is adopted file-by-file; a second ratchet keeps the adopted scope from regressing.
3. **Unit tests (decoupled)** — the 168 Mocha specs are **frozen**; every new test is vitest + TS; legacy specs migrate progressively under a mocha-count ratchet; Mocha is removed once the count reaches zero.

**Definition of Done:**

- [ ] `0` JavaScript files in `lib/` and `bin/` (excluding generated files **and the 3 plugin fixtures under `bin/plugins/available/**`** — see the register, 2026-09-09: the `js` ratchet's floor is therefore `3`, not `0`). _`bin/copy-binaries.js` was long miscounted as a 4th fixture; it is build tooling, and it was converted rather than exempted ([#2713](https://github.com/kuzzleio/kuzzle/pull/2713))._ ⚠️ **Corrected 2026-09-11 ([TD-45](type-debt-register.md#td-45)):** `bin/start-kuzzle-server` and `bin/wait-kuzzle` are extensionless `#!/usr/bin/env node` scripts that `-name '*.js'` never matched, so they had never been counted. `bin/` holds **5** JavaScript files, of which 3 are the fixture floor; both executables are in scope.
- [x] `strict: true` in the main `tsconfig.json`; `allowJs` removed. _(2026-09-21, [step 12](steps/12-sprint-9-strict-flip.md) K6 — `tsconfig.json` is production-only and strict; the specs moved to `tsconfig.tests.json`.)_
- [ ] `0` Mocha specs; vitest + TS the only unit runner; `mocha`/`should`/`rewire`/`c8` removed.
- [ ] Cucumber functional tests unchanged (already TS).

Why this over big-bang, pure-opportunistic, full-strict-now or coupled-tests: [step 00](steps/00-rejected-alternatives.md).

### Consequences

**Easier:** safe refactors (the compiler catches contract breaks) on today's untyped code; one language and one unit runner in the end; autocompletion/DX across the whole core.
**Harder / costlier:** durable CI discipline (four count ratchets, a set ratchet, and a strict scope to maintain); temporary coexistence of two runners and of strict/non-strict `.ts`; the big files (`httpwsProtocol`, `node`, `pluginsManager`) need special care and stronger review.

---

## Target architecture

Stable reference for _how_ the migration is enforced and sequenced. How it was built: [step 01](steps/01-sprint-0-tooling.md).

### Enforcement — five ratchets, and `strict` in the build

- **Count ratchets** (`scripts/ratchet.sh <js|mocha|any|casts>`, `npm run ratchet`): the counts of `.js` files, Mocha specs, written `any` and type assertions may **only decrease**; a reduction updates its baseline in `.migration/` in the same PR. _The `implicit-any` ratchet retired with the strict flip — `strict` subsumes `noImplicitAny`._
- **~~Progressive strict~~ → `strict: true` in `tsconfig.json` (2026-09-21).** For the eight sprints it took to get there, strict was enforced on a growing file list (`.migration/strict-adopted.txt`) by **filtering tsc output** — _not_ via `include`, since tsc pulls the entire import graph into the program. The list reached all of `lib/` and the machinery was deleted with [step 12](steps/12-sprint-9-strict-flip.md)'s K6. **`npm run build` is now the strict type-check**; `npm run typecheck:tests` covers the specs, which have their own non-strict program (`tsconfig.tests.json`) because `strict` applies to a program and not to a file.
- The **written-`any`** ratchet (`any`) exists because explicit `any` is **invisible** to `strict` (see step 01). It counts `: any`, `as any` **and `as unknown as`** — the escape hatch a conversion reaches for once `: any` is forbidden.
- The **implicit-`any`** ratchet (`implicit-any`, `tsconfig.implicit.json`) counts the `TS7xxx` diagnostics over `lib/` + `index.ts` under `noImplicitAny`. It exists because the written-`any` ratchet charges **nothing** for an un-annotated parameter: without it, renaming a file without typing anything scores a perfect zero (see [step 06](steps/06-hardening-mid-course.md)).
- The **`casts`** ratchet (added 2026-09-15, [TD-43](type-debt-register.md#td-43)) counts the **type assertions** in `lib/**/*.ts` — `x as T` and `<T>x` — because `as T` is the hatch the debt moves to once written `any` is ratcheted, and it is the worse of the two: `any` is permissive and visibly untyped, while a _wrong_ `as T` asserts a specific wrong type silently and every gate downstream believes it. `as const` is excluded (it can only narrow to what is already written); `as any`, `as unknown` and both halves of an `as unknown as T` / `as any as T` chain are excluded because the `any` ratchet already charges for them. It is the only ratchet that **parses** rather than greps — see `scripts/count-casts.ts`. Baseline: `.migration/casts-baseline.txt`.
- The **`cpd-exclusions`** ratchet (added 2026-09-11, [TD-23](type-debt-register.md#td-23)) guards `sonar.cpd.exclusions`, the escape hatch a conversion uses when a rename re-scores pre-existing duplication as new code. It is the only one that compares a **set** rather than a count — a count would let a PR swap an entry out for a new one — and it fails on any addition. Baseline: `.migration/cpd-exclusions.txt`.

### Conversion standards (per file)

- Rename `.js` → `.ts`; fix imports/exports (`export`/`import`, or `export =` for a single export, per the existing CommonJS shape).
- **Forbidden in a conversion:** unjustified implicit `any`, `@ts-ignore`/`@ts-nocheck` without a comment + ticket, `!` (non-null assertion) to "make it pass". Prefer real typing or `unknown` + narrowing.
- Reuse and enrich `lib/types`; do not duplicate.
- One PR = one layer (or a coherent subset), small and reviewable, that **decrements the JS baseline**.
- **~~Adopt / report~~ — superseded 2026-09-21 by the flip.** There is no adoption list and no count to report: `strict: true` is in `tsconfig.json`, so a file that does not pass strict does not build. The duty this replaced is worth keeping in view, because it is what the flip cost: for eight sprints a conversion either adopted its files into `.migration/strict-adopted.txt` or stated, per file, the count it left behind and **which of those errors are guards the runtime can reach** — a bug list, not a typing chore. Nothing asked for the second half until [TD-54](type-debt-register.md#td-54), and by then sprints 6 and 7 had left **246** errors across their six largest files, credited wholesale to [TD-53](type-debt-register.md#td-53), of which only 14 were. _A conversion that compiles is not a conversion that checks._
- **A file with no unit spec ships one** (vitest + TS) in its conversion PR: converting untested code is converting blind, and it is the only mechanism that makes the mocha counter fall.
- **No behaviour change in a conversion PR.** Structural refactors stay separate — _except_ the behaviour-preserving ones the SonarCloud new-code gate forces (a rename re-scores the whole file as new code, so pre-existing S2004/S3776 smells must be cleared in-PR). Those are **in scope**, under two conditions: the extraction is **verbatim**, and the step file carries an **equivalence note** stating why behaviour is preserved. See [step 06](steps/06-hardening-mid-course.md) for why the rule is written this way rather than broken every sprint.

### Sequencing (leaves → core)

| Sprint | Target                                                       | Risk       |
| ------ | ------------------------------------------------------------ | ---------- |
| 0      | Tooling & prerequisites (ratchets, strict, `kuzzle-sdk` pin) | Low        |
| 1      | `lib/util` (warm-up)                                         | Low        |
| 2      | `bin/` cleanup + real entrypoints                            | Low/med    |
| 3      | `lib/model`, `lib/service`                                   | Low/med    |
| 4      | `lib/api` (controllers, `funnel`)                            | Medium     |
| 5      | `lib/core` I — storage, security, realtime                   | Med/high   |
| 6      | `lib/core` II — validation, plugin, network/protocols        | **High**   |
| 7      | `lib/cluster` (`node`, `subscriber`)                         | **High**   |
| 8      | `lib/kuzzle`, `index`                                        | Medium     |
| 9      | Final strict flip; remove `allowJs`                          | Medium     |
| 10     | Test closure — legacy Mocha → vitest, remove Mocha           | Spread out |

> Sprints 5–7 run **under the cucumber functional-test net** — the main guarantee against core regressions. The real `strictNullChecks` cost falls on the big JS files (`funnel`, `httpwsProtocol`, `node`, `validation`), not the already-migrated TS; the low `any` count of `lib/cluster` is misleading (un-converted JS, not clean code). Budget sprints 4, 6 and 7 accordingly.
>
> ⚠️ **Sprints 7 and 8 were swapped on 2026-09-15** — `lib/kuzzle` first, `lib/cluster` last. This table is the plan as written on 2026-07-12 and is left as it was; the live order is the [step table](#step-table). The swap was decided on measured coverage: `lib/cluster` holds the only two spec efforts left in the repo, and keeping conversions and spec efforts in separate sprints is what step 09 paid to learn. `index` has since been dropped from sprint 8's target — `index.ts` was converted long before. → [decision register, 2026-09-15](#decision-register)

### Tests

Mocha frozen and running as-is; new tests in vitest + TS; legacy specs migrated progressively (mocha ratchet). **Closure (sprint 10):** at mocha = 0, remove `mocha`, `.mocharc`, `should`, `should-sinon`, `rewire`, `c8` and the `test:unit:mocha` command. The vitest spec location is still an open point (below).

---

## Cold start

> Living section (maintained by the `wrapup` skill). **Read this to resume** in a fresh context.

**Where we are (2026-09-21):** **the language axis is done, and so is the strictness work.** `lib/` holds no JavaScript — eight conversion sprints took the `js` counter **111 → 5**, and the 5 that remain are all in `bin/`: the 3 plugin fixtures that are the floor, plus the two extensionless executables ([TD-45](type-debt-register.md#td-45)).

**[Step 13 — test closure](steps/13-sprint-10-test-closure.md) is the live step**, opened and planned on 2026-09-21. **L0, L1, L1b (b1–b4), L2 (a–e), L3a and L3b have landed** (`mocha` 148 → **54**, vitest 261 → **1 562 tests**). L1b did not exist in the original plan — 30 of the specs L1 was scoped on are built on `test/mocks/kuzzle.mock.js`, which the vitest tree refuses by policy — and the slice table was [re-measured](steps/13-sprint-10-test-closure.md#re-measured-on-2-dev-after-l1b) once it closed. **L2 is closed** — 28 specs in five sub-slices by layer (a: the strays · b: security · c: `core`/`cluster` · d: the `api` controllers · e: the security controllers). ⚠️ **21 of its 28 specs were `KuzzleMock`-based**, so most of L2 was L1b-shaped work (a fixture per spec) rather than a codemod pass — and **5 of L3's 6 are too**. **L3 — the six specs over 1 000 lines — is in flight**, one PR per spec, [ordered by how much of the fixture already exists](steps/13-sprint-10-test-closure.md#how-l3s-6-are-ordered): **L3a `roleRepository` and L3b `securityController/users` have landed**; L3c `request`, L3d `documentExtractor`, L3e `authController`, L3f `documentController` remain. Everything below L3 is done.

**`strict: true` is on in `tsconfig.json`, and [step 12](steps/12-sprint-9-strict-flip.md) is closed.** It opened on **112 unadopted files and 1 673 strict errors** and landed in seven slices — K0 ([#2799](https://github.com/kuzzleio/kuzzle/pull/2799), closing TD-53), K1 ([#2800](https://github.com/kuzzleio/kuzzle/pull/2800)), K2 ([#2801](https://github.com/kuzzleio/kuzzle/pull/2801)), K3 ([#2802](https://github.com/kuzzleio/kuzzle/pull/2802)), K4+K5 ([#2803](https://github.com/kuzzleio/kuzzle/pull/2803)) and K6, the flip ([#2804](https://github.com/kuzzleio/kuzzle/pull/2804)). The step file holds what each one found. **Axis 3 is done. All three axes of this ADR are now closed except test closure ([step 13](#step-table)).**

**There is no progressive-strict machinery any more**: `tsconfig.strict.json`, `tsconfig.implicit.json`, `scripts/strict-check.sh`, `.migration/strict-adopted.txt`, `.migration/implicit-any-baseline.txt`, `npm run test:strict` and the `implicit-any` ratchet are deleted. Nothing is adopted into strict, because nothing is exempt: **`npm run build` IS the strict type-check of `lib/` + `index.ts` + `bin/`**.

⚠️ **K6 was filed as mechanical and was not.** `strict` is a property of a *program*, and the root `tsconfig.json` included the specs: flipping it there put **773 strict errors** (661 `features-legacy`, 53 `tests`, 47 `features`, 12 elsewhere) in the build's path, none of which `--count` had ever reported, because `--count` read a different program. The resolution is **two programs** — `tsconfig.json` (production, strict, what `dist/` is emitted from) and **`tsconfig.tests.json`** (the specs, `strict: false`, `allowJs: true`, `noEmit`), checked by `npm run typecheck:tests` in the `migration-ratchets` job. That is exactly the checking the test code had before the flip; **those 773 are [step 13](#step-table)'s measured starting point.** Read [_What K6 found_](steps/12-sprint-9-strict-flip.md#what-k6-found) before touching either config.

**The build payload went 1 518 → 769 emitted files, and every file `package.json` promises is byte-for-byte there** (`dist/lib/**` 761 before and after, `check-build-payload.sh` green). What left `dist/` is `test/`, `tests/`, `features/`, `features-legacy/`, plus `start-kuzzle-{dev,test}.*` and `.ci/scripts/prepare-coverage.*` — and nothing consumes those from `dist/`: every caller runs them from source through `tsx` or `ts-node/transpile-only`.

⚠️ **One behaviour change from K5 is not covered by a unit spec, and it is now merged:** `Kuzzle.id` was declared and never assigned, so `global.kuzzle.id` read `undefined` everywhere. It is now `global.nodeId`, or a fresh `knode-*` name when no `Backend` named the process. Three carriers go from absent to the node's name — the redis client name (`SETNAME`), the cluster ID card's `id`, and `node` on every realtime notification. It shipped in [#2803](https://github.com/kuzzleio/kuzzle/pull/2803) with the functional suites green; **if a downstream consumer reads any of those three as `undefined`, this is the change that did it.**

**Ratchet state:** `js` 5 · `mocha` **56** · `any` 178 · `casts` 84 · `cpd-exclusions` 6. All five fail-closed since [TD-71](type-debt-register.md#td-71). **`implicit-any` retired with K6** — `strict` subsumes `noImplicitAny`, and its last reading of 30 was an artefact of measuring with `strictNullChecks` off; under the program that builds the package, `lib/` has zero implicit any.

**The foundation is in place and has been re-hardened twice:** 5 CI ratchets, `strict` in the build itself, per-file coverage, ESLint with `consistent-type-imports` as an error, and a `pr-preflight.sh` that reads uncommitted work ([TD-66](type-debt-register.md#td-66)).

**History.** The review-by-review narrative this section used to carry — six reviews, the enforcement built out of them, and how [TD-33](type-debt-register.md#td-33) was closed by _typing_ `publisher.js` rather than by debugging CI — is archived in [journal.md](journal.md). The conclusions, each with what enforces it, are in [lessons.md](lessons.md).

**Counters** (baselines in `.migration/`, re-verified 2026-09-21 on `2-dev` with all of step 12 merged):

| Ratchet              |                 Now |                             At the ADR's open | Floor                                                                             |
| -------------------- | ------------------: | --------------------------------------------: | --------------------------------------------------------------------------------- |
| `js`                 |               **5** |                                           111 | **5 — reached** (3 fixtures + 2 executables, [step 03](steps/03-sprint-2-bin.md)) |
| `mocha` (spec files) |              **56** |                                           168 | 0 — [step 13](#step-table)                                                        |
| `any`                |             **178** | 200 (metric later widened to `as unknown as`) | 0                                                                                 |
| `implicit-any`       |           _retired_ |                                           520 | **retired with K6** — `strict` subsumes `noImplicitAny`                           |
| `casts`              |              **84** |                 87 (ratchet added 2026-09-15) | 0                                                                                 |
| `cpd-exclusions`     |               **6** |                  4 (ratchet added 2026-09-11) | 0                                                                                 |
| strict adopted       |           _retired_ |                                            46 | **the flip happened** — nothing adopted, nothing exempt                           |
| strict errors        |               **0** |                        1 673 (step 12's open) | 0 — reached                                                                       |

Unit tests: **118 Mocha spec files** (**2 758 tests**) against **52 vitest spec files** (**494 tests**) — the ratio [step 13](#step-table) exists to close, and the one number here that has barely moved.

**Remaining JavaScript: 5, all in `bin/`** — 3 plugin fixtures under `bin/plugins/available/**` (the floor, reached by [#2713](https://github.com/kuzzleio/kuzzle/pull/2713)) plus `start-kuzzle-server` and `wait-kuzzle`, two extensionless Node executables the ratchet's predicate had never seen ([TD-45](type-debt-register.md#td-45)). `lib/`: **0**.

**Doc location (2026-07-15):** the ADR and its register moved from `adrs/` to `docs/adr-001/` (`git mv`, history preserved; references updated). ADRs now live under `docs/adr-<n>/`.

**Next actions:**

1. **[Step 13 — test closure](steps/13-sprint-10-test-closure.md) is OPEN (2026-09-21) and is the only step left. It is planned; start at L0.** 148 Mocha specs / 64 295 lines / 3 092 tests against 25 vitest specs / 3 948 lines / 261 tests. The plan partitions the suite exactly, on the axis that decides cost — **not size, but what a spec mocks with**: 96 specs (28 103 lines) use only `should`+`sinon` and are a codemod; **52 specs (36 192 lines) use `mock-require` or `rewire`**, and `rewire`'s 15 are redesigns rather than ports, because reaching a private binding is something vitest deliberately cannot do.
   **L0 is first for a reason worth reading before anything else:** six lib files already have a spec in *both* runners and **three of those ports are unfinished** — `memoryStorageController` sits at **16%** of its Mocha spec, `clientAdapter` at 50%. Deleting those Mocha files is `mocha −1` and a real loss of coverage, and **the ratchet counts files, not tests**. Finish them first; it is the standard the rest is measured against.
   ⚠️ **The 773 strict errors are not one debt**: 708 of them are cucumber (`features-legacy` 661, `features` 47), which is not being migrated and wants its own step. Step 13 owns **65**, due at L7. `bin/` stays paused at its floor ([step 03](steps/03-sprint-2-bin.md)).

2. **[Step 08 — type-debt backlog](steps/08-type-debt-backlog.md), in parallel, is down to two items.** [TD-62](type-debt-register.md#td-62) and [TD-63](type-debt-register.md#td-63) closed through step 12, and [TD-54](type-debt-register.md#td-54) closed with it — **[#2757](https://github.com/kuzzleio/kuzzle/issues/2757) and [#2770](https://github.com/kuzzleio/kuzzle/issues/2770) need closing by hand.** [#2785](https://github.com/kuzzleio/kuzzle/issues/2785) is the design decision [TD-65](type-debt-register.md#td-65)/[TD-67](type-debt-register.md#td-67) left open — _resynchronise instead of evicting_ — and wants a decision before code. **TD-20 stays split and stays blocked:** [#2688](https://github.com/kuzzleio/kuzzle/issues/2688) needs an API shape (`response.configure` cannot set a result), [#2721](https://github.com/kuzzleio/kuzzle/issues/2721) is a breaking HTTP change for the next major.
3. **[#2790](https://github.com/kuzzleio/kuzzle/issues/2790) is a known CI flake — rerun the job, do not diagnose it.** The functional jobs intermittently die in `npm ci` rebuilding native modules. Its local half is fixed ([TD-72](type-debt-register.md#td-72), [#2797](https://github.com/kuzzleio/kuzzle/pull/2797)); the CI half and the functional path stay open.
4. **Per PR, from here on the DoD changes** — no conversions are left, and no adoption list either. Keep **`npm run build` green** (it is the strict type-check of `lib/` + `index.ts` + `bin/`) and **`npm run typecheck:tests`** green (the specs' own program); `npm run ratchet` with **no counter rising** (`casts` and `any` are what stop a "strict fix" from being a cast); ship a spec for any branch a fix adds; run the impacted unit tests **in Docker** (`.ci/scripts/docker-test.sh unit mocha` / `unit vitest` — the native `re2` binding cannot load on host arm64). After merging into `2-dev`, **close the linked issues by hand**: `Closes #NNN` only fires on `master`.

> **Conversion gotchas (learned Sprint 1):** a file whose Mocha spec uses `rewire`/`__set__` on a required module (e.g. `didYouMean`) must keep the compiled variable name — use `import x = require("mod")`, not `import x from "mod"`. Typing a previously-`any` export (e.g. `Promback`) can break inferring consumers: make it **generic** (`Promback<T>`) and annotate the call sites rather than reintroducing `any`.

> **Coverage measurement (learned step 07, PR G3):** the two unit runners disagree on what a measurable line is — `c8` (wrapping Mocha) reports **every line of a loaded file**, comments included, because it maps the compiled output back onto the source; vitest's v8 provider reports only statements. Sonar unions both, so c8's inflated set dominates and **a file tested only in vitest cannot pass the 80% gate**. `.ci/scripts/prepare-coverage.ts` runs before the scan: it drops non-executable lines from both reports, then gives each file a single owner (the runner whose spec targets it, `tests/` mirror convention), never handing over a file vitest measures worse. Reproduce the CI numbers with the four commands in `CONTRIBUTING.md` › _How coverage is measured_.

> **Unit-test gotchas (learned step 07):** (1) **`import x = require(...)` is unloadable under vitest** — it emits a real `require()` vite cannot resolve, so any spec importing that module dies on `Cannot find module`. Use a **default import**, which type-checks against `export =` under `esModuleInterop` and works in both worlds. (2) **Convert, then write the spec** — never the reverse: vite cannot resolve a still-`.js` module's CommonJS `require` graph. (3) `vi.mock` factories are hoisted above the module body, so anything they close over must come from `vi.hoisted`. (4) A local mocha ∪ vitest coverage estimate is **not** a usable proxy for `new_coverage`: the two providers instrument different line sets (comments, braces), so a naive union under-reports. Read the number off the PR's SonarCloud analysis.

> **Unit-test gotchas (learned step 06):** (1) `vitest.config.ts` must **not** set `test.root` — coverage paths resolve against it, which sends the lcov to the wrong directory _and_ caps the instrumented scope to the spec tree, so `lib/` is never measured (the vitest report was empty for as long as the runner existed). Select specs with `include` instead. (2) In a spec, a module exported with `export =` needs a **default import** (`import bytes from "…"`); `import bytes = require("…")` type-checks but does not resolve at runtime under vite. (3) A converted file **absent from the coverage report is more likely dead than untested** — check for callers first (`lib/util/wildcard.ts` had none).

> **Conversion gotchas (learned Sprint 4):** (1) the right import form depends on the **target's** export shape, not on a repo-wide convention — a barrel shipping `export = { … }` cannot be reached by a named ES import (`TS2497`; use `import x = require(…)` + destructuring), while a still-`.js` module cannot be reached by `import x = require(…)` (use a default import under `esModuleInterop`). (2) `git mv` matters: adding the `.ts` while leaving the `.js` tracked breaks nothing locally (every consumer uses the extensionless specifier) but **fails the js ratchet in CI** — run `npm run ratchet:js` before pushing. (3) Converting a **hub** file costs more than its own LOC: giving its collections a real element type newly type-checks every consumer (PR E2's `Map<string, NativeController>` cascaded into `serverController` and `realtimeController`).

**Key commands:**

```bash
npm run ratchet                     # js / mocha / any / casts / cpd-exclusions
npm run build                       # tsc + copy-binaries — AND the strict type-check of lib/
npm run typecheck:tests             # tsconfig.tests.json — tests/, test/, features/, features-legacy/
npm run build:tests                 # emits dist/test/** — the Mocha suite's input
npm run ratchet:js -- --update      # after a reduction: update the baseline
.ci/scripts/pr-preflight.sh         # lint + error-codes + ratchets/strict + 2 reminders
```

**Conversion convention:** `export =` for a single export, named exports for an object module; never `any` / `@ts-ignore` / `!` to "make it pass"; reuse `lib/types`; adopt the file into strict if clean; ship a vitest spec if it had none. Full standards in _Target architecture › Conversion standards_.

---

## Step table

The spine. One row per unit of work; `Detail` links to the frozen/living step file.

| #   | Step                                                                          | Status                              | PR(s)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Detail                                       |
| --- | ----------------------------------------------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| 00  | Rejected alternatives (decision rationale)                                    | 🧊 Archive                          | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | [detail](steps/00-rejected-alternatives.md)  |
| 01  | Sprint 0 — tooling & foundations (ratchets, strict, `kuzzle-sdk` pin)         | ✅ Done                             | #2668, #2669                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [detail](steps/01-sprint-0-tooling.md)       |
| 02  | Sprint 1 — `lib/util` warm-up (100% TS)                                       | ✅ Done                             | #2670, #2674                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [detail](steps/02-sprint-1-util.md)          |
| 03  | Sprint 2 — `bin/` cleanup (entrypoints deprioritized)                         | 🟦 Paused                           | #2671                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [detail](steps/03-sprint-2-bin.md)           |
| 04  | Sprint 3 — models & services (100% TS)                                        | ✅ Done                             | #2676                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [detail](steps/04-sprint-3-model-service.md) |
| 05  | Sprint 4 — `lib/api` (controllers, `funnel`) — **100% TS**                    | ✅ Done                             | #2679 A · #2680 B · #2681 C · #2682 D · #2685 E1 · #2686 E2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [detail](steps/05-sprint-4-api.md)           |
| 06  | Mid-course hardening (enforcement before `lib/core`)                          | ✅ Done                             | #2689 F1 · #2693 F2 · #2694 F3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [detail](steps/06-hardening-mid-course.md)   |
| 07  | Sprint 5 — core I (storage, security, realtime, cache, shared) — **100% TS**  | ✅ Done                             | [#2695](https://github.com/kuzzleio/kuzzle/pull/2695) G1 · [#2696](https://github.com/kuzzleio/kuzzle/pull/2696) G2 · [#2698](https://github.com/kuzzleio/kuzzle/pull/2698) G3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [detail](steps/07-sprint-5-core-i.md)        |
| 08  | Type-debt backlog, worked in parallel with the sprints                        | 🟦 In progress                      | TD-26 [#2699](https://github.com/kuzzleio/kuzzle/pull/2699) · TD-21 [#2700](https://github.com/kuzzleio/kuzzle/pull/2700) · TD-23 [#2701](https://github.com/kuzzleio/kuzzle/pull/2701) · TD-22 [#2702](https://github.com/kuzzleio/kuzzle/pull/2702) · TD-27 [#2709](https://github.com/kuzzleio/kuzzle/pull/2709) · TD-28 [#2710](https://github.com/kuzzleio/kuzzle/pull/2710) · TD-31 [#2711](https://github.com/kuzzleio/kuzzle/pull/2711) · TD-29 [#2712](https://github.com/kuzzleio/kuzzle/pull/2712) · TD-30 [#2713](https://github.com/kuzzleio/kuzzle/pull/2713) · TD-32 [#2716](https://github.com/kuzzleio/kuzzle/pull/2716) · TD-34/35 + TD-36/37 [#2717](https://github.com/kuzzleio/kuzzle/pull/2717) | [detail](steps/08-type-debt-backlog.md)      |
| 09  | Sprint 6 — core II (validation, plugin, network) — **100% TS**                | ✅ Done                             | [#2722](https://github.com/kuzzleio/kuzzle/pull/2722) H1 · [#2723](https://github.com/kuzzleio/kuzzle/pull/2723) H2 · [#2724](https://github.com/kuzzleio/kuzzle/pull/2724) H3 · [#2746](https://github.com/kuzzleio/kuzzle/pull/2746) H4 · [#2748](https://github.com/kuzzleio/kuzzle/pull/2748) H5 · [#2750](https://github.com/kuzzleio/kuzzle/pull/2750) H6                                                                                                                                                                                                                                                                                                                                                       | [detail](steps/09-sprint-6-core-ii.md)       |
| 10  | Sprint 7 — `lib/kuzzle` (bootstrap, vault, dumps, event runner) — **100% TS** | ✅ Done                             | [#2752](https://github.com/kuzzleio/kuzzle/pull/2752) I1 · [#2753](https://github.com/kuzzleio/kuzzle/pull/2753) I2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [detail](steps/10-sprint-7-kuzzle.md)        |
| 11  | Sprint 8 — `lib/cluster` (the last conversion sprint) — **100% TS**           | ✅ Done                             | [#2772](https://github.com/kuzzleio/kuzzle/pull/2772) J0 · [#2775](https://github.com/kuzzleio/kuzzle/pull/2775) J1 · [#2780](https://github.com/kuzzleio/kuzzle/pull/2780) J2 · [#2791](https://github.com/kuzzleio/kuzzle/pull/2791) J3a · [#2792](https://github.com/kuzzleio/kuzzle/pull/2792) J3b · [#2794](https://github.com/kuzzleio/kuzzle/pull/2794) J4                                                                                                                                                                                                                                                                                                                                                     | [detail](steps/11-sprint-8-cluster.md)       |
| 12  | Sprint 9 — final strict flip, remove `allowJs`                                | ✅ Done | [#2799](https://github.com/kuzzleio/kuzzle/pull/2799) K0 · [#2800](https://github.com/kuzzleio/kuzzle/pull/2800) K1 · [#2801](https://github.com/kuzzleio/kuzzle/pull/2801) K2 · [#2802](https://github.com/kuzzleio/kuzzle/pull/2802) K3 · [#2803](https://github.com/kuzzleio/kuzzle/pull/2803) K4+K5 · [#2804](https://github.com/kuzzleio/kuzzle/pull/2804) K6                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | [detail](steps/12-sprint-9-strict-flip.md)   |
| 13  | Sprint 10 — test closure (Mocha → vitest): 148 spec files, 64 295 lines       | 🟦 Open — **L0, L1, L1b, L2, L3a, L3b done** (148 → 54), L3c–L3f next | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | [detail](steps/13-sprint-10-test-closure.md) |

---

## Decision register

Canonical "what we decided", one line each. Links point to the step that details it.

- **2026-09-21** — **Step 13, L1: c8 and vitest's v8 provider do not share a denominator, so their percentages cannot be compared.** Same file, same moment: c8 reports 173/377 lines and 16/17 branches where v8 reports 22/93 and 13/44 — and vitest covers *more* functions (6 vs 5) while showing a lower percentage. Seven of twelve subjects looked like regressions and none was one. This is [step 07](steps/07-sprint-5-core-i.md)'s finding — the reason `prepare-coverage.ts` normalises both before Sonar — applied to the comparison L0's own DoD asked for. _The arbiter is the normalised report; what a port owes plainly is that every `it` it replaces is accounted for._ → [13](steps/13-sprint-10-test-closure.md)

- **2026-09-21** — **Step 13, L1: the axis a slice is cut on is a hypothesis, and this is the third time it was wrong.** L1 was scoped as "the specs using neither `mock-require` nor `rewire`", and half of them turned out to depend on a third thing: `test/mocks/kuzzle.mock.js`, a ~600-line application stub the vitest tree refuses on purpose. 30 specs had no slice at all; **L1b** now exists for them. K6 sliced on a flag and missed the tools reading the config; L0 sliced on lines and missed what coverage says. _Measure the whole population against the axis before committing to it._ → [13](steps/13-sprint-10-test-closure.md)

- **2026-09-21** — **Step 13, L1: a word that means different things by type is where a codemod lies.** `should(fn).throw(Err, { id })` became `toThrow(Err, { id })`, and vitest's `toThrow` takes one argument and **silently ignores the second** — 22 assertions quietly halved, in a suite that stayed green. Only `tsc` saw it (TS2554), which is what the test program is type-checked for. Same shape for `.match()` (string vs deep partial) and `.be.empty()` (length vs own keys); the last was removed from the translation table rather than fixed. _A codemod may refuse to translate; it may not guess._ → [13](steps/13-sprint-10-test-closure.md)

- **2026-09-21** — **Step 13, L0: a line count compares two texts; only coverage compares two tests.** L0 was scoped on the line ratio between a Mocha spec and its vitest counterpart, and that instrument was wrong on **two of three** subjects, in both directions. `clientAdapter`'s vitest spec is 948 lines against 1 903 because it is **table-driven**, and it covers **100%** of the subject where the Mocha spec covered **24.1%** — a better port accused of being a partial one; it was deleted outright. `stackTrace` was **never a pair at all**: the two files test different functions of the same module, and they matched only because **macOS's filesystem is case-insensitive** (`stacktrace.test.js` vs `stackTrace.test.ts`) — on CI's Linux the pairing would never have been made. Only `memoryStorageController` was what it was filed as. _Anything pairing `test/x` with `tests/x` has to be case-sensitive, and the arbiter of a port is the coverage report._ → [13](steps/13-sprint-10-test-closure.md)

- **2026-09-21** — **Step 13's perimeter is the unit suites; cucumber is not part of it.** K6 left `tsconfig.tests.json` holding 773 strict errors and the cold start called them step 13's starting point. **708 of them are cucumber** (`features-legacy` 661, `features` 47), which is not being migrated to anything — bundling them into a Mocha → vitest step would make its ratchet meaningless, since the `mocha` counter cannot move for work done in `features-legacy/`. Step 13 owns **65**. The cucumber debt wants its own step, recorded here so the number is not lost. → [13](steps/13-sprint-10-test-closure.md)

- **2026-09-21** — **Step 13 is sliced on what a spec mocks with, not on how big it is.** `should` → `expect` and `sinon` → `vi` are a codemod, and 96 of the 148 specs (28 103 lines) need nothing else. The other 52 (36 192 lines) use `mock-require` — dynamic, where `vi.mock` is hoisted and static — or `rewire`, which reaches a module's **private** bindings and has no vitest equivalent **by design**. _A `rewire` spec is not a port but a redesign: it says the test was written against an implementation detail, so fixing it is a change to `lib/`._ → [13](steps/13-sprint-10-test-closure.md)

- **2026-09-21** — **A ratchet that counts files can be satisfied by deleting coverage, and this one nearly was.** Six lib files have a spec in both runners; three of those ports are unfinished — `memoryStorageController`'s vitest spec is **16%** of its Mocha original, `clientAdapter`'s is 50%. Deleting the Mocha file reads as `mocha −1` and loses real assertions. What has kept it honest is `prepare-coverage.ts` refusing to hand a file to the runner that measures it worse — a coverage gate, not a permission. Step 13's L0 is finishing those three, and its DoD adds _report tests, not files_. → [13](steps/13-sprint-10-test-closure.md)

- **2026-09-21** — **Step 12, K6: `strict` is a property of a program, so the flip is a decision about `include`, not about a flag.** The step cleared 1 673 errors measured through `tsconfig.strict.json` (`lib/` + `index.ts`); the root `tsconfig.json` also included `tests/`, `test/`, `features/` and `features-legacy/`, where the same flag reports **773** errors — none of which `--count` had ever printed, because it read a different program. `npm run build` is a bare `tsc`, so the flip as filed does not produce a strict repository, it produces one that does not build. **Two programs**: `tsconfig.json` is production-only, strict, and is what `dist/` is emitted from; `tsconfig.tests.json` carries the specs with exactly the checking they had before (`strict: false`, `allowJs: true`), run by `npm run typecheck:tests`. _A measurement is scoped to the program it was taken in, and a config file's `include` is part of the measurement._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-21** — **Step 12, K6: a build-payload gate derived from `package.json`'s `files` cannot see a consumer that is not a package consumer.** Narrowing the production `include` dropped 749 emitted files; `check-build-payload.sh` was green, because every file the *package* promises was still there. But `.mocharc` globs `./dist/test/**/*.test.js` — the 148-file Mocha suite runs on emitted JavaScript, and CI's recipe is `npm run build && npm run test:unit:mocha` — so the change had silently removed the suite's entire input, three config files away from anything the gate reads. `tsconfig.tests.json` emits into the same `dist/` via `npm run build:tests`, which `test:unit:mocha` now runs first. _Running the suites is what caught it; no gate derived from the package's promises could have._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-21** — **Step 12, K5: hoisting an indexed read out of its call site drops the receiver, and the hoist is exactly what strict asks for.** `noUncheckedIndexedAccess` types `client[command]` as `T | undefined`, so the natural fix is to read it into a local and guard — which turns `client[command](...args)` into `implementation(...args)` and loses `this`. ioredis' commands live on the `Commander` prototype and read `this.options`: **every redis command threw, and Kuzzle did not boot.** The unit suites passed, because they stub the client; all 30 functional shards and both Build-and-Run jobs failed. This is the second telling — sprint 5's `funnel.doAction`, whose comment says to use `Reflect.apply`, is the first. The other three hoists in this slice were audited and made faithful, including one where the *original* passed no receiver and the fix had started handing it one. _When strict makes you hoist a call, the receiver is part of what you are preserving._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-21** — **Step 12, K4/K5: declaring what a name is finds defects that reading the code does not.** Typing `kerror.get`'s first argument as the error name — one signature — made two unreachable error codes fall out on the same day: both Elasticsearch wrappers declare one mapping entry `subCode` where every other says `subcode` and the reader asks `subcode`, so `services.storage.too_many_operations` was unreachable; and `redis.ts` asks for `notconnected` where the code is `services.cache.not_connected`, so every command issued against a downed cache raised `core.fatal.unexpected_error`. Neither is visible by reading the file — both are visible the moment a string stops being a string. _The same argument as [TD-68](type-debt-register.md#td-68)'s: a name that crosses a boundary untyped is a claim nobody checks._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-21** — **Step 12, K4/K5: post-`init()` state is an accessor, not a nullable field dereferenced on faith.** Forty-odd sites across the protocol stack, the cluster command layer, the cluster node and the plugins manager declared a field nullable, filled it in `init()`, and read it unchecked everywhere after. Each became a getter that throws once, where it can actually fail — K3's `Redis.connectedClient` generalised. **Where a spec assigns the field, the accessor keeps a setter:** replacing a field with a read-only accessor is a behaviour change to every test double, and three suites said so. _The alternative — a guard at each call site — writes a branch describing a state the runtime cannot be in, which the step's DoD forbids._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-21** — **Step 12, K5: `request.context` and `request.input` are getters, so checking one of their properties never narrows the next read of it.** One line of TypeScript semantics, ~40 errors across `authController`, `funnel` and `pluginContext` — and the reason each of those files looked far more nullable than it is. The fix is to read once into a local, or to ask a helper that throws (`userOf`, `tokenOf`, `targetOf`). _Worth writing down because the diagnosis is not in the error message: the compiler says "possibly null" at a line whose author did check._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-21** — **Step 12, K4: a deferral written into a type is a decision with a trigger, and the trigger fires.** `bindPluginMethod` was annotated `PluginMethod | undefined` with a comment saying the dead branch's fate was _"to decide the day this file joins `strict-adopted`"_. That day was K4. It throws now, and its three callers keep no guard for a value that cannot exist. _A `@todo` rots; a deferral expressed as a type, with the condition that resolves it named in the comment, gets resolved._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-21** — **Step 12, K4/K5: `satisfies` is the answer when a literal table is both checked and read back by key.** Annotating `memoryStorageController`'s Redis command table `RedisCommandMapping` is what contextually typed its `map` closures — and what made every read-back `CommandArguments | undefined`, so eighteen aliases at the bottom of `initMapping` could not be assigned. `satisfies` does the checking without widening. Same for `kerror`'s nine domains and `loadPluginsErrors` reaching into `domains.plugin`. ⚠️ **Under `noImplicitAny` alone, a lone `null` property of an inferred literal is `any`** — ten of them, which the implicit-any ratchet counts; they are a named `NO_MAPPING` constant. → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-20** — **Step 12, K3: a ratchet that can never be overridden is one that gets deleted the first time it is wrong.** Taking both `elasticsearch.ts` to strict-clean re-scored their pre-existing ≈57% twin duplication as new code — 36.7% against a 5% gate — and no work inside the slice moves a number that is the files' own density. [TD-23](type-debt-register.md#td-23)'s "`sonar.cpd.exclusions` may only shrink" met [TD-16](type-debt-register.md#td-16)'s "deduplicating the per-major adapters is a non-goal", with the sibling `esWrapper.ts` pair already excluded for that exact reason. The list goes **4 → 6**, recorded as a deliberate exception with its cost stated — CPD is now off for the two largest files in `lib/`. _The override is cheap; what makes it honest is that moving the baseline is a diff a reviewer sees, with the argument written next to it._ → [12](steps/12-sprint-9-strict-flip.md), [TD-23](type-debt-register.md#td-23)

- **2026-09-20** — **Step 12, K3: a narrowing that discards data is a behaviour change wearing a type's clothes.** `_catAliases` was written to drop `cat.aliases` rows missing the `alias`/`index` the SDK declares optional and Elasticsearch always fills. Six specs failed at once — the fixtures answer rows with `alias` alone. It defaults both to `""` instead, and an empty alias fails the index-prefix test every caller already applies, which is what a missing one used to throw on. _When a type says "this cannot be absent", the honest fix is a total value, not a `filter`._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-20** — **Step 12, K3: declaring an untyped module honestly is a defect-finding exercise, not paperwork.** `ms` and `semver` were implicitly `any` in both `elasticsearch.ts`. Writing `lib/types/ms.d.ts` and `lib/types/semver.d.ts` — local declarations rather than `@types` packages, K1's `didyoumean.d.ts` precedent, because a dependency change is not a typing slice's to make — immediately surfaced two silent holes: `semver.coerce` answers `null` for an unparsable version and was fed straight into `satisfies`; `ms` answers `undefined` for an unreadable duration and was fed straight into a `>`. _Declare only what you call: an unused declaration is a claim nothing checks._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-20** — **Step 12, K3: with near-identical twins, take one to zero before touching the other.** ES7 and ES8's services differ by 616 lines of 7 736. ES8 went first and cost six red specs on one helper alone; ES7 then went **197 → 0 in a single pass with no test failures**, because every lever arrived already proven and already written down. Fixing both in parallel would have meant diffing two moving files. _The second of a pair is a porting exercise only if the first is finished._ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-19** — **Step 12, K2: a private-field convention the compiler cannot see is a typing blocker, and moving it is a public-API decision.** `lib/api/request` keys every backing field with a zero-width space appended (`input\u200b`) so `console.log(request)` prints the public name — 121 of the slice's 133 errors. The keys are now **declared as class members** rather than renamed to `private _input` or `#input`: both of those move the runtime key, and `console.log`, `util.inspect`, `JSON.stringify` and reflection over a request are what a plugin author sees. Declaring changes nothing at runtime and cost zero collateral. Removing the masquerade stays available as its own decision. → [12](steps/12-sprint-9-strict-flip.md)

- **2026-09-19** — **Step 12, K1: a file's error count does not predict whether it can be fixed alone.** 54 of the 57 files at ≤ 5 errors were local; the other three were the visible end of a type living elsewhere, and answering them in place _relocates_ errors instead of removing them — `lib/model/security`'s `_id: string = null` moves **+18** into the auth and security repositories (measured, then reverted). `--count` cannot tell the two apart, so the slicing metric needs a second question: _is this file's type its own?_ → [12](steps/12-sprint-9-strict-flip.md)

- **2026-07-12** — Migrate **incrementally** (CI ratchet + per-layer sprints, leaves → core); reject big-bang and pure-opportunistic. → [00](steps/00-rejected-alternatives.md)
- **2026-07-12** — Treat the migration as **three decoupled-but-coordinated axes** (language priority · progressive strict · frozen Mocha). → [00](steps/00-rejected-alternatives.md)
- **2026-07-12** — Adopt `strict` **file-by-file**, enforced by **filtering tsc output** (not tsconfig `include`, which pulls the whole graph); flip global `strict: true` only at the end. → [01](steps/01-sprint-0-tooling.md)
- **2026-07-12** — **Freeze** the 168 Mocha specs; every new unit test in **vitest + TS**; migrate legacy progressively under a mocha-count ratchet. → [01](steps/01-sprint-0-tooling.md)
- **2026-07-12** — Add a **3rd ratchet on explicit `any`** (invisible to `noImplicitAny`/`strictNullChecks`); re-enable `@typescript-eslint/no-explicit-any` as `warn`. → [01](steps/01-sprint-0-tooling.md)
- **2026-07-12** — **Bound the `kuzzle-sdk` pin** (`>=7.17.1 <8`) + snapshot the root-exported surface; keep the intentional `export * from "kuzzle-sdk"` re-export. → [01](steps/01-sprint-0-tooling.md)
- **2026-07-15** — **Deprioritize Sprint 2** (`bin/` entrypoints); only the dead-code removal (#2671) shipped, the rest waits behind the `lib/` layers. → [03](steps/03-sprint-2-bin.md)
- **2026-07-15** — **Relocate ADRs** from `adrs/` to `docs/adr-001/`; the repo convention is now `docs/adr-<n>/`.
- **2026-07-17** — **Split Sprint 4 into 3 PRs** (A: clean controllers + helpers · B: big controllers + server · C: `funnel`/`httpRoutes`/barrel), leaves → dispatch. → [05](steps/05-sprint-4-api.md)
- **2026-07-17** — **PR A** (#2679): 6 `lib/api` files → TS (`export =`, no new `any`); js baseline 79 → 73. `documentExtractor` split out to PR B — a `.js`→`.ts` rename makes SonarCloud score the whole file as new code, and its pre-existing deprecated-API calls (`setResult` options / `getArrayLegacy`) then fail the `0 New Issues` gate. Companion JSDoc fix in `validation.js` (DOM `Request` → `KuzzleRequest`). → [05](steps/05-sprint-4-api.md)
- **2026-07-20** — **PR C** (#2681): `memoryStorageController` → TS (`export =`, no new `any`); js 72 → 71. First converted controller whose Mocha spec drives `rewire` — kept `mapping`/helpers as top-level bindings + `export =` (no `__esModule` wrapper) so `__get__`/`__set__` resolve on the compiled CJS. Pre-existing intra-file duplication (the `mapping` table + repeated arg-extraction closures) CPD-excluded. → [05](steps/05-sprint-4-api.md)
- **2026-07-21** — **PR D** (#2682): `serverController` + `documentExtractor` → TS (`export =`, no new `any`, 0 logic change); js 71 → 69. The planned "migrate off `setResult`/`getArrayLegacy`" proved **behaviour-changing** (`response.configure` can't set the result and forces status 200; `getArray` throws where legacy `getArrayLegacy` `split(",")`s) → **kept the deprecated calls with `// NOSONAR`** and deferred the real (breaking) migration to a dedicated PR ([TD-20](type-debt-register.md)). Cross-layer type fixes: `kuzzle.statistics` `private`→`public`; `config.version` modelled (TD-18, partial). → [05](steps/05-sprint-4-api.md)
- **2026-09-07** — **PR E1** (#2685, `chore/ts-migration-sprint4-api-routes`): `httpRoutes` + `controllers/index` → TS (`export =`, no new `any`, 0 logic change); js 69 → 67, `lib/api` down to `funnel.js` alone. **Standardized every native controller on `export =`**: `adminController`, `authController` and `securityController` were still `export default class`, whose `__esModule` + `exports.default` shape forced the `new XController.default()` workaround in `funnel.js` and in 10 spec call sites — all removed. `httpRoutes` gets a module-local `KuzzleHttpRoute` interface (`url?` because the trailing loop derives it from `path`). `default.config.ts` had to drop the `.js` extension from its import: node10 module resolution does **not** substitute `.ts` for a `.js` specifier. `HttpConfiguration.routes` deliberately left `any` — typing it cascades into already-converted files. → [05](steps/05-sprint-4-api.md)
- **2026-09-07** — **PR E2** (#2686, `chore/ts-migration-sprint4-funnel`): `funnel` → TS (`export =`, no new `any`, 0 logic change); js 67 → 66 — **`lib/api` is 100% TypeScript, Sprint 4 converted**. `PendingRequest` + the module helpers kept as top-level bindings so `execute.test.js`'s `rewire(…).__get__("PendingRequest")` still resolves; the `export =` barrel imported via `import apiControllers = require("./controllers")` + destructuring (a named ES import cannot target `export =`). Typing `this.controllers` as `Map<string, NativeController>` **cascaded** into `serverController._buildApiDefinition` (`_actions` is a `Set`, not `string[]`) and `realtimeController.validate` — both fixed type-only. **[TD-18](type-debt-register.md) closed** (`internal.allowAllOrigins` modelled); `HttpConfiguration.accessControlAllowOrigin` widened to `string | string[] | RegExp[]` to match what `config/index.ts` actually produces. **[TD-21](type-debt-register.md) opened**: `_wrapError` passes the _request_ to `isNativeController(name)`, so the guard is always false and every non-`KuzzleError` is wrapped as a plugin error — a latent bug the conversion **preserved** (two specs assert it). → [05](steps/05-sprint-4-api.md)

- **2026-09-09** — **Sprint 4 closed** (#2686 merged): `lib/api` is 100% TypeScript; js baseline 111 → 66 since the ADR opened. → [05](steps/05-sprint-4-api.md)
- **2026-09-09** — **Mid-course review of the 30 converted files** → open [step 06](steps/06-hardening-mid-course.md) and **harden the enforcement before `lib/core`**. Governing rule: _a review finding ends as a ratchet, an adopted-list entry or a GitHub issue — never as prose alone._ → [06](steps/06-hardening-mid-course.md)
- **2026-09-09** — Add a **4th ratchet on implicit `any`** (`TS7xxx` under `noImplicitAny`, baseline 520) and widen the written-`any` ratchet to `as unknown as` (200 → 208, a broadened metric and not a regression). Rationale: the written-`any` ratchet charges nothing for an un-annotated parameter, so it cannot tell a conversion from a rename. → [06](steps/06-hardening-mid-course.md)
- **2026-09-09** — **Strict adoption becomes part of a conversion PR's DoD** when the file is clean (46 → 94 adopted; `--candidates` emptied), enforced by a `pr-preflight` reminder. Fixed `strict-check.sh`, whose unanchored path match let a bare `index.ts` entry capture every `lib/**/index.ts` error. → [06](steps/06-hardening-mid-course.md)
- **2026-09-09** — **A conversion PR ships a vitest spec for a file that had none.** 30 files were converted with 0 tests written, 6 of them with no spec at all; deferring every spec to sprint 10 leaves conversions unnetted. → [06](steps/06-hardening-mid-course.md)
- **2026-09-09** — **Gate-driven, behaviour-preserving refactors are IN scope for a conversion PR** (verbatim extraction + an equivalence note in the step file). A rename re-scores the whole file as new code, so the SonarCloud gate forces them; the previous absolute "structural refactors stay separate" was being broken every sprint in silence. → [06](steps/06-hardening-mid-course.md)
- **2026-09-09** — **Restoring `.ts` coverage (PR F2) gates the start of Sprint 5.** `sonar.coverage.exclusions=**/*.ts` means every conversion removes its file from the coverage gate; converting the 50 files of `lib/core` under a vacuous gate is not acceptable. → [06](steps/06-hardening-mid-course.md)
- **2026-09-09** — **The plugin fixtures under `bin/plugins/available/**` are out of the Definition of Done**: `functional-test-plugin` and `kuzzle-plugin-cluster` are **fixtures**, not product code. "0 `.js` in `bin/`" targets `bin/` proper. → [03](steps/03-sprint-2-bin.md) ⚠️ **Corrected 2026-09-10:** the entry said "the 4 remaining", but only **3** are fixtures — `bin/copy-binaries.js` is build tooling, so the `js` floor is **3**, and that file is still in scope ([#2705](https://github.com/kuzzleio/kuzzle/issues/2705)).

- **2026-09-09** — **Sprint 5 closed** ([#2698](https://github.com/kuzzleio/kuzzle/pull/2698) merged): `lib/core`'s storage, security, realtime, cache and shared layers are 100% TypeScript; js 66 → 50. → [07](steps/07-sprint-5-core-i.md)
- **2026-09-09** — **A file is measured by the runner that owns its spec.** `c8` and vitest's v8 provider instrument different line sets and Sonar unions them, so c8's inflated set (every line of a loaded file, comments included) made a vitest-tested file unable to clear the 80% gate. `.ci/scripts/prepare-coverage.ts` drops non-executable lines from both reports, then hands each file to one runner — never one vitest measures worse. → [07](steps/07-sprint-5-core-i.md)
- **2026-09-09** — **The type-debt register is burned down as a parallel track**, not inside the sprints: small single-purpose PRs, one finding each. Opened [step 08](steps/08-type-debt-backlog.md); TD-21, TD-22, TD-23 (partial) and TD-26 closed the same day. → [08](steps/08-type-debt-backlog.md)
- **2026-09-09** — **`sonar.cpd.exclusions` shrinks for the first time**: `documentController.ts` deduped (`_mFetch` / `_writeDocument`, verbatim extraction + equivalence note) and removed from the list. 4 entries left, of which 3 are declared irreducible. → [08](steps/08-type-debt-backlog.md)
- **2026-09-10** — **Post-sprint review of the five merged PRs**: direction confirmed, five follow-ups filed ([#2703](https://github.com/kuzzleio/kuzzle/issues/2703)–[#2707](https://github.com/kuzzleio/kuzzle/issues/2707)). Two rules come out of it: **a guard belongs at the error's source, not on a shared funnel** (TD-21 changed plugin-pipe behaviour it never meant to touch), and **an error reaching a client must keep a documented `id`/`code`** — letting one through unwrapped hands it to `setError`, which builds an `InternalError` with neither. → [08](steps/08-type-debt-backlog.md)
- **2026-09-10** — **`bin/copy-binaries.js` is converted, not exempted**, and run through `tsx` from the source tree (`npx tsx ./bin/copy-binaries.ts`) rather than as compiled output from `dist/` — the script's own `__dirname` is what resolves the repository root, and `tsx` is already how the repo runs `.ci/scripts/prepare-coverage.ts`. `bin/` reaches its floor of 3 fixtures. → [08](steps/08-type-debt-backlog.md)
- **2026-09-10** — **Closing an issue is part of `wrapup`, not of merging**: GitHub resolves `Closes #NNN` only on the default branch (`master`), so PRs merged into `2-dev` leave their issues open. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **An exclusion list is a ratchet or it is debt.** `sonar.cpd.exclusions` gains a **fifth ratchet** (`cpd-exclusions`, baseline `.migration/cpd-exclusions.txt`). It is the first one that compares a **set** rather than a count: a count would let a PR swap an entry out for a new one, which is exactly the move the rule forbids. The rule itself had been written in `sonar-project.properties` since step 06 and enforced nothing — the ADR's own governing rule is that a finding ends as a ratchet, an adopted-list entry or an issue, **never as prose alone**. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **A readiness probe must exercise the thing the caller depends on.** `bin/wait-kuzzle` resolved on the SDK's `connected` event — proof the transport listens, not that the cluster has quorum. It now sends a real request and treats any answer other than `api.process.not_enough_nodes` as ready: `funnel.throttle()` is the single gate every request passes, so with `minimumNodes=3` a node that answers _is_ a node in quorum — no credentials and no `cluster:status` call needed. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **The SDK's auto-reconnect cannot be used as a first-connection retry.** `WebSocketProtocol.onclose` forwards to `clientNetworkError()` — the only thing that schedules a retry — solely `if (this.wasConnected)`. A socket accepted then closed before the first successful connection (a published Docker port whose container is still booting) matches no branch: `connect()` neither resolves nor rejects, and nothing retries. Any readiness probe built on the SDK must own its retry loop. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **A `@deprecated` tag that names a replacement must be checked against the replacement.** Both of TD-20's deprecations point at APIs that cannot do the job — `response.configure()` takes no result, and `getArray` rejects the comma-separated strings `getArrayLegacy` accepts — so for two sprints the tags have produced `NOSONAR` markers instead of migrations. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **Plan a sprint on the normalised coverage report, never on raw `c8`.** `prepare-coverage.ts` drops blank and comment-only lines in _both_ directions, so normalisation is not a free uplift (repo-wide 84.4% → 80.2%). On sprint 6's 34 candidates it moves three files across the 80% threshold — planning off the raw report would have shipped them as gate-safe conversions. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **A boolean validator every caller follows with a property read is an undeclared type guard.** `BaseType.checkAllowedProperties` and `safeObject.isPlainObject` both proved their argument was a plain object and then returned `boolean`, so each caller had to cast to use what had just been proven. Declaring the predicate (`o is Record<string, unknown>`) removed every cast in H1 at no runtime cost. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **A `hasOwnProperty` in validation code is load-bearing.** Rewriting it as a truthiness test reads better and is what TS narrows on, but the two differ exactly on the malformed specifications those methods exist to reject: four Mocha specs pin `{ range: undefined }` and `{ range: { min: undefined } }` as throwing. Use `has()` for presence, and `!== undefined` only where TS genuinely needs the narrowing. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **A per-type options shape needs a generic base class, not one wide interface.** `range` means `{ min?: number }` to `numeric` and `{ min?: Moment | "NOW" }` to `date`; a union makes every comparison a type error. `BaseType<TOptions>` carries the shape, and method parameter bivariance keeps every subclass assignable to the bare `BaseType` the type registry stores. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **A JSDoc type in an unconverted file is a name lookup in that file's scope, not a type.** `entryPoint.js`'s `@param {Request}` resolves to the **DOM** `Request` (`lib.dom` is in tsconfig's `lib`, and the file imports no `Request`), so inferring the entry point's type would have checked every protocol against `fetch`'s Request. H2 declares `NetworkEntryPoint` instead. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **A `@deprecated` line in a JSDoc block is never about one parameter.** Written for `Protocol.init`'s deprecated `name` argument, it sat as a block tag and deprecated the whole method — every `super.init(...)` then scored an S1874 on the gate. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **When a spec forces a non-idiomatic import, fix the spec.** `mock-require` keys on the literal specifier, so a spec registering `"net"` never intercepts `require("node:net")` — 11 red tests. Registering both names in the spec is two lines and keeps the source idiomatic; dropping the `node:` prefix only moves the problem to SonarCloud (S7772). → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **A block that lands under the coverage threshold is naming the file that is not really tested.** H3 measured 76.1%; four assertions would have cleared the gate and netted nothing. Writing the two specs the arithmetic pointed at — one file had none, the other's Mocha spec stubbed the base class it was supposed to exercise — took the block to 92.2% and the mocha counter down for the first time. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **`ObjectRepository<TObject>` cannot express a nullable `load()`.** `PluginRepository.load()` resolves `null` for a missing user, which is documented behaviour, and declaring it against the base's `Promise<TObject>` needs a double cast. The file is left out of strict rather than cast; whoever revisits the base class should start there. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **A dead parameter is worse than a removed one: it survives a conversion as a type.** `Protocol.init`'s deprecated `name` argument has thrown since #1645 (2020-06-16) — `assert(this.name && !name)` requires `this.name` truthy, so no caller can use it — and the first TypeScript conversion widened the parameter to `string | null` to accommodate it, advertising a call that crashes. The overload now says `null`, the only second shape the method can accept. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **Fix the type where the defect is, not where it became visible.** `PluginRepository.load()` resolving `null` against a `Promise<TObject>` base looked like a subclass problem; `ObjectRepository`'s own `load`, `loadFromCache` and `loadOneFromDatabase` all had a `return null` path. Widening the base costs nothing while `strictNullChecks` is off — `T | null` collapses to `T`, `tsc` output unchanged — and is the whole difference at step 12. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **A ratchet measures its predicate, not its label.** "`.js` files under `lib/` and `bin/`" and `-name '*.js'` are not the same set: two extensionless `#!/usr/bin/env node` executables in `bin/` had never been counted, so the floor was wrong by two and #2719 could add 100 lines of JavaScript where the invariant forbids it. The baseline goes **up**, 20 → 22 — the instrument was wrong, not the code, and freezing a known-false floor to keep a number monotonic is the worse trade. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **A check that reads its verdict out of another process's stdout must prove that process ran.** `strict-check.sh` derived "all adopted files pass" from the absence of `error TS` in a log it never checked was produced. The exit status cannot settle it either — tsc returns 2 both for "errors in your code" and "could not read your config" — so the discriminator is the shape of the output: a config-level `^error TS` line means nothing was checked. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **A job's `if:` is inherited by everything downstream of it.** `functional-tests` declared `needs: [sonarqube]` for sequencing; `sonarqube` is fork-gated; a skipped `needs` skips its dependents, so 30 functional jobs and 6 monkey jobs never ran on a pull request from a fork — nor did the build-payload gate TD-35/36/37 took three iterations to get right. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **A tool's scope is a glob written once, and the tree it names is the one being deprecated.** ESLint and Prettier covered `./test` and never `./tests`, where ADR-0001 sends every new spec — 15 files with no lint and no formatting, and `prettier` was `--write`-only with no `--check` anywhere. Any rule aimed at "new code" has to name where new code goes. → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **An aggregate threshold prices the block and says nothing about its worst member.** H3 landed at 76.1% and the arithmetic named the two files that were not really tested; H2 landed at 88.3% and three files shipped with no spec at all, one of them receiving a bug fix in the same PR. Four latent bugs were fixed in H2 and none shipped a regression test. The rule has to be per file. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-11** — **"No cast" is a proxy for "no unchecked claim", and a wrong type is the same claim made more quietly.** `PluginRepository.load()` declares `Promise<PluginDocument>` and resolves `null`; refusing the double cast the base class would have needed did not avoid the lie, it moved it out of the `any` ratchet's sight and into a file excluded from strict. When a standard cannot be met, the honest move is the visible violation. → [08](steps/08-type-debt-backlog.md)
- **2026-09-14** — **A type-only import written as a value import is a runtime dependency, not a style preference.** `tsc` elides it, so nothing in the type system complains, while at runtime it pulls the target's whole transitive graph into every consumer — `lib/types/Global.ts` alone put the backend barrel → `kuzzle` → `lib/cluster/*.js` behind every module that touches `global.kuzzle`. `@typescript-eslint/consistent-type-imports` is an **error** repo-wide. → [08](steps/08-type-debt-backlog.md)
- **2026-09-14** — **`lib/` does not import the package's own `index.ts`.** The barrel re-exports the modules importing it, so a leaf takes on the whole public surface to name one type. Import the module that defines what is used. → [08](steps/08-type-debt-backlog.md)
- **2026-09-14** — **A finding that counts its own instances has already chosen a predicate, and the predicate is the claim.** TD-49 was filed as "25 `import x = require()`"; it was one class with four spellings, three of which that grep could not match, and the count was the least interesting thing about it. Same lesson as TD-45, from the opposite direction. → [08](steps/08-type-debt-backlog.md)
- **2026-09-15** — **A coverage number is the output of a merge, and a merge is a claim about what two measurements have in common.** Sprint 6's H4 was sized as a +390 covered-line spec effort on `validation.js` (36.2%); the file is at **99.2%** and there was no work to do. 43 specs use `mock-require`'s `reRequire`, so V8 compiles those modules several times and c8 merges the resulting `ScriptCoverage` entries at the **range** level, before conversion, losing coverage — for `validation.js` it reported 508 of 1180 statements when its own largest single input held 995, and an istanbul-level merge of the same raw data holds 1171. `.ci/scripts/merge-coverage.ts` now re-derives the report; overall mocha coverage **81.4% → 92.7%**. Two things worth keeping: the gate had been failing **closed**, so nothing shipped on a false pass — what it cost was _plan_, two PRs sized and three classified against debt that was not there; and the tell had been sitting in the report from the first day, needing no new tool — **93% of branches next to 36% of lines**, which no real file can have, since a branch cannot be exercised on a line that never ran. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-15** — **A ratchet that greps counts English.** Third instance of the same lesson, and the first where the predicate was wrong in _both_ directions: TD-43 filed the `casts` metric as `as [A-Z]` with a count of **16**; that regex matches **84** times over `lib/`, most of them `import * as Cookie from …` or prose ("tracked as TD-43", "Kuzzle ships as CommonJS"), while the real number of assertion nodes is **87**. A predicate that fires on a comment does not just miscount — it teaches contributors to reword comments to get a PR through, which is the opposite of what a ratchet is for. So the sixth ratchet **parses**: `scripts/count-casts.ts` walks the TypeScript AST and counts `AsExpression`/`TypeAssertionExpression` nodes, which also makes it the first ratchet to count _nodes_ rather than matching lines — two assertions on one line cost two. Its exclusions are the interesting half and each is a deliberate statement: `as const` (cannot assert anything wrong), and `as any` / `as unknown` / both halves of a double cast (already the `any` ratchet's, and charging twice would mean two baselines to update for one fix). → [08](steps/08-type-debt-backlog.md)
- **2026-09-11** — **Sprint 6 opened** ([step 09](steps/09-sprint-6-core-ii.md)): 34 files, 8 195 LOC, six PRs — three conversions on the 26 gate-safe files (aggregate 94.3%) and three spec-efforts-then-rename on `validation.js`, `plugin`+`pluginsManager` and `entryPoint`+`httpwsProtocol`. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-15** — **Sprint 6 closed**: `lib/core` holds no JavaScript (H4 [#2746](https://github.com/kuzzleio/kuzzle/pull/2746), H5 [#2748](https://github.com/kuzzleio/kuzzle/pull/2748), H6 [#2750](https://github.com/kuzzleio/kuzzle/pull/2750)). js **49 → 17**, strict **102 → 131**. The three spec efforts the sprint was planned around never existed — [TD-50](type-debt-register.md#td-50) was. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-15** — **A gate-driven refactor is not done when the local checks pass.** Two findings survived H5's first cleanup into a second SonarCloud analysis, and neither is visible locally: an extraction that lifts a function out _whole_ moves its cognitive complexity rather than reducing it (`resolveVerifiedUser` was still at 17, needing a second split), and a `// NOSONAR` on a leading comment line is **ignored** — the marker has to sit on the flagged line, which Prettier will take away from a call it wraps. The rule that follows is procedural: the gate is the oracle, so `wrapup` waits for the real analysis rather than for a green `npm run lint`. → [09](steps/09-sprint-6-core-ii.md)
- **2026-09-15** — **Sprint 7 is `lib/kuzzle`, not `lib/cluster`** — the reverse of the order this hub had carried since sprint 0, decided on the measurement rather than on the layer. `lib/kuzzle` is 6 files / 550 measurable lines at **95.9%**, nothing below 88%; `lib/cluster` is 6 files / 1 544 lines at **88.9%** and holds the only two spec efforts left in the repo (`command.js` **16.9%**, `workers/IDCardRenewer.js` **71.3%**). Keeping conversions and spec efforts in separate sprints is the lesson step 09 paid for: a sprint framed as both gets re-planned mid-flight. → [10](steps/10-sprint-7-kuzzle.md)
- **2026-09-16** — **A conversion's definition of done must name the strict count it leaves behind.** The DoD has always named coverage, because coverage is what the gate scores; nothing named the number that says whether the new TypeScript _checks_ anything, so nobody produced it. Measured after the fact ([TD-54](type-debt-register.md#td-54), [#2757](https://github.com/kuzzleio/kuzzle/issues/2757)), sprints 6 and 7 left **246** strict errors across their six largest files — and only **14** are the config shape ([TD-53](type-debt-register.md#td-53)) the step docs had credited the whole exclusion to. The rule that follows: every conversion PR reports `npx tsc -p tsconfig.strict.json --noEmit` filtered to the files it converted, and either adopts them into `.migration/strict-adopted.txt` or states, per file, what the remaining errors are and which of them is a guard the runtime can reach. _A ratchet only counts what someone was asked to report._ → [TD-54](type-debt-register.md#td-54)
- **2026-09-17** — **Sprint 8 J1: converting a file found what six CI investigations had not.** `index.js` and `publisher.js` → TS (`export =`, no behaviour change, js **11 → 9**), and reading the publisher to type it produced [TD-65](type-debt-register.md#td-65) ([#2773](https://github.com/kuzzleio/kuzzle/issues/2773)): the handshake takes a node's `lastMessageId` over the **command** socket and resumes the **sync** subscription from it, while ZeroMQ registers that subscription asynchronously on a different connection. Anything published in between is dropped by the publisher and read as a desync by the subscriber, which self-evicts. That is [TD-33](type-debt-register.md#td-33)'s surviving half — _one message, at formation, any node_ — and none of it is in the membership logic five reviews searched. **The generalisable part is the method, not the bug:** the step's _Risks_ section had already pointed at `publisher.js` from the CI evidence alone; what it could not produce was the mechanism, and typing the file did. This is the third sprint in a row where the conversion, not the investigation, surfaced the defect (`maxFormFileSize` in 6, a `Promise` the declaration denied in 7). → [11](steps/11-sprint-8-cluster.md)
- **2026-09-17** — **TD-65 and TD-67 fixed: TD-33's cluster half is closed.** A joining node now proves its sync subscription is live — `waitForSubscription()` resolves on the first message received from a peer, and `handshake()` awaits it for every peer **before** `getFullState` snapshots their `lastMessageId`. No protocol change: heartbeats already flow every 2 s from before a node is discoverable. And `evictSelf` now shuts the node down instead of only broadcasting an eviction that a `PUB` socket cannot deliver to its own process — which is why an evicted node went on answering requests behind the load balancer from state that had stopped advancing. Two smaller defects fell out of the same reading: `sync()` applied the rest of its buffer after a gap had evicted the node and then overwrote `EVICTED` with `SANE`, and the handshake's success line was printed unconditionally, 2 ms after the eviction contradicting it. **A residual race is named rather than hidden**: the resume point still comes from whichever node answered the fullstate, so a third party lagging behind this node's first received message would reopen a much narrower window — closing it means resynchronising instead of evicting, which is a design decision left open. → [08](steps/08-type-debt-backlog.md), [11](steps/11-sprint-8-cluster.md)
- **2026-09-17** — **[TD-33](type-debt-register.md#td-33) closed ([#2715](https://github.com/kuzzleio/kuzzle/issues/2715)), after nine occurrences and five reviews.** Worth recording as a method, because almost none of the work that closed it was debugging. Three _diagnostics_ had to land before the defect was even legible — [TD-58](type-debt-register.md#td-58) (a gap under-reported by one, so every single-message loss printed `0 messages lost` and was dismissed as spurious), [TD-59](type-debt-register.md#td-59) (two unrelated `knode-*` ids per node, so no log line could be attributed to a container) and the log dump being armed for the test run rather than only the readiness gate. Then the mechanism came from **typing `publisher.js`**, not from reading CI. _A defect that survives five investigations is usually not hidden — it is illegible, and the cheapest fix is to the instruments._ → [08](steps/08-type-debt-backlog.md), [11](steps/11-sprint-8-cluster.md)
- **2026-09-18** — **Sprint 8 closed: `lib/` holds no JavaScript, and the language axis of this ADR is done.** Six PRs (J0 [#2772](https://github.com/kuzzleio/kuzzle/pull/2772), J1 [#2775](https://github.com/kuzzleio/kuzzle/pull/2775), J2 [#2780](https://github.com/kuzzleio/kuzzle/pull/2780), J3a [#2791](https://github.com/kuzzleio/kuzzle/pull/2791), J3b [#2792](https://github.com/kuzzleio/kuzzle/pull/2792), J4 [#2794](https://github.com/kuzzleio/kuzzle/pull/2794)); `js` **11 → 5** (the `bin/` floor), strict **135 → 141**, mocha 149 → 148, and **neither `any` counter nor `casts` moved across 2 843 converted lines** — the layer was typed without buying a single hatch. → [11](steps/11-sprint-8-cluster.md)
- **2026-09-18** — **The strict-count DoD worked, and it is not enough.** [TD-54](type-debt-register.md#td-54)'s rule made every sprint-8 PR report what it left behind: 65 errors over four files, each attributed at the moment it was created — against 246 that sprints 6 and 7 left on six files because nobody asked. What the rule cannot do is make a conversion _fix_ them. _A definition of done can force a measurement; only a step can force the work._ Which is why [step 12](steps/12-sprint-9-strict-flip.md) opens with a measured backlog rather than a survey. → [11](steps/11-sprint-8-cluster.md), [12](steps/12-sprint-9-strict-flip.md)
- **2026-09-18** — **Step 12 opened on a measurement, and the measurement corrected the plan's own premise.** 112 unadopted files, **1 673** strict errors: 36% nullability, 27% assignability, 22% implicit `any`. [TD-53](type-debt-register.md#td-53) — the config shape the step docs had credited the exclusions to for two sprints — is **83 errors, about 5%**. It is a real blocker (it demonstrably keeps `idCardHandler.ts` out of strict) and a small one, which is [TD-54](type-debt-register.md#td-54)'s claim confirmed repo-wide rather than over six files. Slicing follows the distribution, not the layers: half the debt is in ten files, half the files carry a tenth of it. → [12](steps/12-sprint-9-strict-flip.md)
- **2026-09-18** — **`allowJs` is not blocked by the Mocha suite, contrary to what the step order assumed.** `tsconfig.json` includes `test/**/*.js`, so removing `allowJs` looked like it had to wait for step 13; compiling with `allowJs: false` **type-checks clean today** — the JavaScript specs simply drop out of the program and nothing imports them. The emitted payload still has to be diffed before the flip ([`check-build-payload.sh`](../../.ci/scripts/check-build-payload.sh), which exists because this class of change broke the package once). _A dependency between two steps is worth measuring before it is planned around._ → [12](steps/12-sprint-9-strict-flip.md)
- **2026-09-18** — **[TD-71](type-debt-register.md#td-71): a ratchet's test is "did the tool run", never "is the count small".** With `node_modules` half-installed, `ratchet:implicit-any` reported `0 < baseline 455` and printed the command that would have written 0 into the baseline and disarmed it permanently. This is [TD-44](type-debt-register.md#td-44) — fixed in `strict-check.sh` seven weeks earlier — in the two sibling scripts nobody re-checked. Both now fail closed ([#2795](https://github.com/kuzzleio/kuzzle/pull/2795)). _When a finding is about a class of tool, the fix is not done until every instance of that class has been enumerated._ → [TD-71](type-debt-register.md#td-71)
- **2026-09-18** — **[TD-72](type-debt-register.md#td-72): the local Docker unit runner was installing into the bind-mounted host tree**, leaving Linux native builds — or nothing — in a macOS checkout, which is how TD-71 was found. It now installs into a named volume ([#2797](https://github.com/kuzzleio/kuzzle/pull/2797)). The functional path cannot be fixed the same way (the cluster services must see the dependencies) and [#2790](https://github.com/kuzzleio/kuzzle/issues/2790) stays open for it and for the CI `npm ci` flake — **which is a known flake: rerun, do not diagnose.** → [TD-72](type-debt-register.md#td-72)
- **2026-09-18** — **The hub's cold start is a cold start again.** It had grown to 60 lines of review narrative — the thing the `kuzzle-adr` skill exists to keep out of a hub — so the narrative moved verbatim to [journal.md](journal.md) and the section is back to state, counters and next actions. _A living section that is never shortened stops being read, which costs exactly what the structure was adopted to prevent._

- **2026-09-18** — **Step 12, K0: one type serving two directions always resolves in favour of the looser one.** `KuzzleConfiguration` described both what a user may write and what `global.kuzzle.config` holds, so every reader in `lib/` was checked against the partial. Split into three — `IKuzzleConfiguration` (merged, total), `PackagedKuzzleConfiguration` (what the defaults file ships, i.e. everything but the two sections startup derives) and `KuzzleConfiguration` (the user's, now a **deep** partial, which is what `rc` and `_.merge` actually accept). **1 673 → 1 567 strict errors, 4 files adopted**, including the two sprint 7 left out for exactly this reason. Three things the fix found: the defaults file was never total; the public partial was shallow and therefore unusable as an override type; and `security.jwt` was optional on a shape that always carries it. `loadConfig(): IKuzzleConfiguration` is a claim nothing else can check — every reader is checked _against_ it — so two specs pin it, verified to bite. → [12](steps/12-sprint-9-strict-flip.md), [TD-53](type-debt-register.md#td-53)

---

## Open points

- **Validate this ADR** with the core team (status Proposed → Accepted).
- ~~**vitest spec location**~~ — **settled (2026-09-09, step 06 PR F3):** `tests/` mirrors the source tree, discovery `tests/**/*.{test,spec}.ts`, recorded in `CONTRIBUTING.md` › _Where unit tests live_.
- **Strict flag order** — revisit (per-flag vs per-file) based on the pain actually observed.
- **Own `JSONObject` server-side** (`lib/types/JSONObject.ts`) and codemod the ~44 `from "kuzzle-sdk"` type imports; remove the 2 local storage redefinitions — during the `lib/types` sprint. (Register: TD-09 / TD-10.)
- ~~**Sprint 8 is the last conversion sprint**~~ — **settled (2026-09-18, [step 11](steps/11-sprint-8-cluster.md)):** it was, it applied the strict-count DoD on all four converted files, and it found [TD-33](type-debt-register.md#td-33)'s mechanism by typing `publisher.js`.
- **Resynchronise instead of evicting** ([#2785](https://github.com/kuzzleio/kuzzle/issues/2785)) — [TD-65](type-debt-register.md#td-65)'s residual race is narrow but real, and closing it is a **design** decision about cluster recovery, not a fix. Open, unscheduled.
- **What replaces the migration machinery at the flip** — `strict-check.sh`, `tsconfig.strict.json` and `strict-adopted.txt` all disappear in [step 12](steps/12-sprint-9-strict-flip.md)'s K6. The `any`, `implicit-any` and `casts` ratchets do **not**: their floor is 0 and they are at 204 / 455 / 87. Decide whether they survive the ADR's closure as permanent gates or get their own follow-up effort.
- **Closure** — remove `allowJs`, Mocha and the legacy config once the counters reach zero (a closing ADR if warranted).

---

## References

- **Companion:** [lessons](lessons.md) — every _generalisable part_ recorded by this ADR, one line each, with what enforces it. The 📝 rows are the backlog of what to gate next; a review that files a finding adds its row.
- **Companion:** [type-debt register](type-debt-register.md) — detailed, tracked findings of the 2026-07-12 audit (the ADR sets the strategy; the register tracks the execution).
- **Companion:** [journal](journal.md) — the cold-start narrative the hub carried up to 2026-09-18, archived when the language axis closed. History, not state.
- **Steps:** [`steps/`](steps/) — one file per milestone; `00` archives the rejected alternatives, `01`–`07` and `09`–`11` cover the eight conversion sprints (all closed and frozen), `08` is the parallel type-debt track (open), `12` is the step in flight.
- **Process tooling:** the `kuzzle-adr` skill (this hub + steps structure) and the `wrapup` skill (keeps this document live).
- ADRs live under `docs/adr-<n>/` — distinct from `doc/` (reserved for the Kuzzle documentation tool).
