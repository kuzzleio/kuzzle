name: Install Packages
description: Install necessary packages inside the CI

runs:
  using: "composite"
  steps:
    - run: sudo apt update && apt install libunwind-dev libunwind8 -y
      shell: bash
