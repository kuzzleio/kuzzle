---
name: getting-started-first-steps#init
description: Creates a new index and collection
hooks:
  before: |
    curl -XDELETE kuzzle:7512/playground
  after: |
    curl -XDELETE kuzzle:7512/playground
template: empty
expected: playground/mycollection ready

sdk: js
version: 6
