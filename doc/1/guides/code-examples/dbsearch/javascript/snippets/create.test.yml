name: guidedbsearch#create
description: Creates documents
hooks:
  before: |
    curl -XDELETE kuzzle:7512/nyc-open-data
  after:
template: default
expected: New documents successfully created!
sdk: js
version: 6
